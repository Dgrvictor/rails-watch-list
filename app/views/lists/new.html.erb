<div class="container">
  <h1>New List</h1>

  <div>
    <%= simple_form_for(@list) do |f| %>
      <%= f.input :name %>
      <%= f.button :submit %>
    <% end %>
  </div>
  <%= link_to "Back to lists", lists_path %>
</div>
